# Archivo gitignore  

Un archivo .gitignore es un archivo de configuración que se usa en proyectos que trabajan con Git (sistema de control de versiones). Su función es indicarle a Git qué archivos o carpetas debe ignorar, es decir, cuáles no debe seguir ni subir al repositorio.

## 📌 ¿Qué es exactamente?

Es un archivo de texto llamado .gitignore (con punto al inicio).
Dentro se escriben reglas que le dicen a Git:

“Estos archivos existen en mi proyecto, pero no quiero que formen parte del control de versiones.”

## 🎯 ¿Para qué se utiliza?

En un proyecto de programación hay muchos archivos que no es necesario compartir o que incluso pueden causar problemas si se suben. El .gitignore se usa para evitar eso.